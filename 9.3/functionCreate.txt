CREATE FUNCTION "AskAQuestion" (@question varchar(50))
returns varchar(50)
AS
BEGIN
declare @value varchar(50);
set @value=('42')
return @value;
END;
GO
SELECT dbo.AskAQuestion('Koji je odgovor na pitanje zivota i smrti?')